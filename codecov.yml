codecov:
  token: f5837286-d823-4015-8513-a870212598ac
  branch: master
  bot: null

coverage:
  precision: 2
  round: down
  range: "70...100"

  status:
    project:
      default:
        target: auto
        if_no_uploads: error

    patch:
      default:
        if_no_uploads: error

    changes: true


comment:
  layout: "header, diff, changes, suggestions"
  behavior: default
